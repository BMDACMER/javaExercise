/**
 * ??????????????????????????????????????????????????? ????0???????????????????????????0
 */

/**
 * ?¡¤????????????????????????????????§Ö??????????????????????????????????????????????????????????
 * ?????????????????num = num * 10 + charArray[i] - '0'?????????????????????????charArray[i]??????
 * ?????????????§Ø??????????????????????????

 ????????????????1????????§Ñ????§Ù????????????2????????§Ñ??????????????3??????????????????
                ??4????????§³???????????5???????
 ????????????????????????????????
 ????????https://blog.csdn.net/u013132035/article/details/80466925
 */
public class Test50 {
    public static int StrToInt(String str) {
        if (str == null || str.length() < 1)
            return 0;
        int result = 0;
        boolean negative = false;//?????
        int i = 0, len = str.length();
        /**
         * limit ???????? ???? ????????? ??????????????????????
         * ???result(?????????????????)???????????????????????????
         * ?§Ø???????
         */
        int limit = -Integer.MAX_VALUE;
        int multmin;
        int digit;

        if (len > 0) {
            char firstChar = str.charAt(0);//????????¦Ë
            if (firstChar < '0') { // Possible leading "+" or "-"
                if (firstChar == '-') {
                    negative = true;
                    limit = Integer.MIN_VALUE;//?????????????§Ø????????????? ?????? ??§³??????
                } else if (firstChar != '+')//???¦Ë?????????-?????+
                    return 0;
                if (len == 1) // Cannot have lone "+" or "-"
                    return 0;
                i++;
            }
            multmin = limit / 10;
            while (i < len) {
                // Accumulating negatively avoids surprises near MAX_VALUE
                digit = str.charAt(i++)-'0';//char?int
                if (digit < 0 || digit > 9)//0??9?????????
                    return 0;

                //?§Ø????
                if (result < multmin) {
                    return 0;
                }
                result *= 10;
                if (result < limit + digit) {
                    return 0;
                }
                result -= digit;
            }
        } else {
            return 0;
        }
        //??????????????-result??result?????????
        return negative ? result : -result;
    }

    public static void main(String[] args) {
        System.out.println(StrToInt("21"));
    }
}